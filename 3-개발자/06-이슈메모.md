# 이슈메모

- 문서 최종 갱신일자: 03/14 16:32

## 1. 목적
- 설계 충돌, 일정 지연, 보류 이슈, 의사결정 필요사항을 기록한다.

## 2. 기록 원칙
- 이슈 발생 시 배경, 영향 범위, 대응 필요사항을 함께 남긴다.
- 설계 판단이 필요한 이슈는 문서로 남긴 뒤 처리한다.

## 3. 현재 상태
- 이슈 01. 기본 포트 6001 충돌
  - 배경
    - 기존 루트 경로 서버(`/Users/jaehojoo/Desktop/codex-lgcns/sattie/venv/bin/uvicorn app.sattie_api:app --host 0.0.0.0 --port 6001 --reload --reload-dir app`)가 6001 점유 중
  - 영향
    - `src/one-shot-startup.sh` 기본 설정과 충돌
  - 필요 조치
    - 필요 시 기존 서버 정리 또는 다른 포트 사용
- 이슈 02. 상세 화면 렌더링 문법 오류
  - 배경
    - 상세 화면의 카테고리 문서 영역 렌더링 중 comprehension 내부 walrus 표현식 사용으로 `SyntaxError` 발생
  - 영향
    - `src/app/doc_service/service.py` import 실패
    - `src/app/sattie_api.py`까지 연쇄 import 실패
  - 처리 결과
    - 문법 오류 제거
    - HTML 닫는 태그 구조 정리
    - 컴파일 및 TestClient 재검증 완료
- 이슈 03. ERD 문서 내부 상대 링크 미해결
  - 배경
    - `src/guide/modeling/0.integrated-ERD.md`의 목록 링크가 `./1.customer-account-domain.md` 같은 상대 경로로 작성되어 있음
    - 상세 화면 렌더러는 이를 `/p/{slug}` public URL로 바꾸지 않아 사용자가 링크를 따라가면 문서를 찾지 못함
  - 영향
    - `0. 통합 ERD`
    - `1. 고객 계정 관리 ERD`
    - `2. 위성 자산 관리 ERD`
    - `3. 궤도 지상국 운영 ERD`
    - `4. 촬영 요청 Feasibility 평가 ERD`
    - `5. 스케줄링 실행 통제 ERD`
    - `6. 데이터 상품 다운링크 전달 ERD`
    - `7. 감사 추적 ERD`
    - 위 링크가 문서 서비스에서 정상 이동하지 않음
  - 처리 결과
    - 현재 문서의 `source_path` 기준으로 상대 링크를 해석해 대상 문서의 slug를 찾고 `/p/{slug}`로 재작성하도록 보정
    - `/p/0-integrated-erd` 기준 전체 링크 변환 검증 완료
