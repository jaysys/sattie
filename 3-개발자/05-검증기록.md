# 검증기록

- 문서 최종 갱신일자: 03/14 17:35

## 1. 목적
- 기능 검증, QA, 회귀 검증 결과를 누적 기록한다.

## 2. 기록 원칙
- 기능 단위 검증 결과를 날짜/대상/결과/비고 기준으로 기록한다.
- 실패 항목은 이슈메모와 연결해 관리한다.

## 3. 현재 상태
- 정적 검증 결과
  - `python3 -m py_compile src/app/doc_service/__init__.py src/app/doc_service/models.py src/app/doc_service/indexer.py src/app/doc_service/seo.py src/app/doc_service/service.py src/app/sattie_api.py` 통과
- 런타임 검증 결과
  - `src/one-shot-setup.sh`는 `pip/setuptools/wheel` 업그레이드를 기본 생략하도록 보정 후 성공
  - `src/one-shot-startup.sh`는 `RELOAD=0` 기본값, 포트 충돌 확인, health check 기반 성공 판정으로 보정
  - `PORT=6002 bash ./one-shot-startup.sh` 기준 startup 성공 확인
  - `src/logs/sattie.log` 기준 `/health` 200 응답 기록 확인
  - `bash ./one-shot-stop.sh` 기준 stop 동작 확인
  - FastAPI `TestClient` 기준 아래 MVP 경로 200 응답 확인
    - `/`
    - `/health`
    - `/hub`
    - `/hub/category/{category}`
    - `/upload`
    - `/api/docs/list`
    - `/api/docs/search?q=api`
    - `/p/{slug}`
    - `/api/docs/{slug}`
    - `/api/docs/{slug}/raw`
  - `DOCS_ADMIN_TOKEN` 설정 후 `/admin/docs` 200 응답 확인
  - `DOCS_ADMIN_TOKEN` 설정 후 `POST /api/docs/reindex` 200 응답 확인
  - 상세 페이지 부가 기능 문자열 확인
    - `copy-btn`
    - `heading-copy`
    - `imgModal`
  - ERD 문서 내부 상대 링크 보정 확인
    - 대상 원본 파일: `src/guide/modeling/0.integrated-ERD.md`
    - 확인 링크: `0. 통합 ERD`, `1. 고객 계정 관리 ERD`, `2. 위성 자산 관리 ERD`, `3. 궤도 지상국 운영 ERD`, `4. 촬영 요청 Feasibility 평가 ERD`, `5. 스케줄링 실행 통제 ERD`, `6. 데이터 상품 다운링크 전달 ERD`, `7. 감사 추적 ERD`
    - 상세 페이지 `/p/0-integrated-erd` 렌더링 결과에서 위 링크들이 모두 `/p/...` public URL로 변환됨을 확인
  - 업로드 화면 기능 검증
    - `POST /api/docs/preview` 200 응답 확인
    - `/upload` HTML에 파일 선택 input, 미리보기 영역, preview 스크립트 반영 확인
    - `POST /api/docs/upload` 기준 업로드 201 응답 확인
